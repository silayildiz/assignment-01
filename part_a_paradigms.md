## Snippet 1
**Paradigm**: Imperative programming

**Explanation**: This snippet uses a variable `total` as an accumulator and updates it step by step inside a `for` loop. The code describes exactly how the calculation should happen: start from zero, visit each expense, add its amount, and then print the final result.

## Snippet 2
**Paradigm**: Procedural programming

**Explanation**: This snippet organizes the program into named functions such as `get_total` and `get_by_category`. Each function performs a specific task and can be reused, which is a typical feature of procedural programming.

## Snippet 3
**Paradigm**: Imperative programming

**Explanation**: This snippet builds the `totals` dictionary by directly changing its contents during a loop. It uses explicit control flow and state updates to calculate the category totals before finding the most expensive category.

## Snippet 4
**Paradigm**: Functional programming

**Explanation**: This snippet uses functional-style tools such as `sum`, `map`, `filter`, `lambda`, and a generator expression. Instead of manually updating variables in a loop, it expresses the transformations and filtering operations more directly.
